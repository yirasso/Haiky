# Agent harness config, not project source. Without these, .claude/settings.json
# and .mcp.json are indexed as code and graphify ends up describing its own
# hook wiring instead of the app's.
.claude/
.mcp.json

# Generated icons and installers are already in .gitignore, listed again here
# so a run with --no-gitignore still skips them.
dist/
build/icon.png
src/assets/tray-*.png

# Not listed, deliberately:
#   graphify-out/          already skipped by the scanner, except:
#   graphify-out/memory/   indexed on purpose — saved answers become graph nodes
#                          on the next `graphify update .`, which is the feedback
#                          loop working, not corpus pollution.
